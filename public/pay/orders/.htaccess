# Never serve order records over HTTP. (Best practice is to point orders_dir
# ABOVE the web root in config.php; this is the fallback protection.)
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
